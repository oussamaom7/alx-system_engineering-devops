#!/bin/bash

# Function to display the message and sleep
display_message() {
    echo "To infinity and beyond"
    sleep 2
}

# Trap Ctrl+C to exit gracefully
trap 'echo "Exiting..."; exit' INT

# Infinite loop
while true; do
    display_message
done

exit 0
